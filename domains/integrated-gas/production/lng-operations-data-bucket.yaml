apiVersion: platform.shell.com/v1alpha1
kind: StorageBucket
metadata:
  name: lng-operations-data
  namespace: default
spec:
  compositeDeletePolicy: Background
  parameters:
    acl: private
    provider: azure
    region: asia-east
    versioning: true
