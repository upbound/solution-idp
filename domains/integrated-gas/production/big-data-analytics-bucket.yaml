apiVersion: platform.shell.com/v1alpha1
kind: StorageBucket
metadata:
  name: big-data-analytics-bucket
  namespace: default
spec:
  compositeDeletePolicy: Background
  parameters:
    acl: private
    provider: aws
    region: europe-west
    versioning: true
